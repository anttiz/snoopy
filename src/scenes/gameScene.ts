import { BaseScene } from "./baseScene";

const sceneIndex = 1;
export class GameScene extends BaseScene {
  constructor() {
    super({ sceneIndex });
  }

  /*
  init(data: any) {
  }
  */

  preload() {
    super.preload();
  }

  create() {
    super.create();
  }

  update() {
    super.update();
  }
}
